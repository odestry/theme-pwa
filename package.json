{
  "name": "shopify-theme-pwa",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@8.1.1",
  "scripts": {
    "dev": "shopify theme dev -s odestry",
    "lint": "shopify theme check",
    "pull": "shopify theme pull -d -o sections/*.json templates/*.json config/*.json"
  },
  "devDependencies": {
    "@shopify/cli": "latest",
    "@shopify/theme": "latest"
  }
}
